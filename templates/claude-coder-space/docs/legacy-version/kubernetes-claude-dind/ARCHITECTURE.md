# kubernetes-claude-dind Architecture

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        K3s Kubernetes Cluster                    │
│                                                                   │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                  Namespace: coder                         │  │
│  │                                                           │  │
│  │  ┌──────────────┐              ┌──────────────┐         │  │
│  │  │   Coder      │◄─────────────┤  PostgreSQL  │         │  │
│  │  │   Control    │              │   Database   │         │  │
│  │  │   Plane      │              └──────────────┘         │  │
│  │  └──────┬───────┘                                        │  │
│  │         │                                                │  │
│  └─────────┼────────────────────────────────────────────────┘  │
│            │                                                    │
│            │ Manages                                            │
│            ▼                                                    │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │              Namespace: coder-workspaces                  │  │
│  │                                                           │  │
│  │  ┌─────────────────────────────────────────────────────┐ │  │
│  │  │              Workspace Pod (Envbox)                 │ │  │
│  │  │                                                     │ │  │
│  │  │  ┌────────────────────────────────────────────┐    │ │  │
│  │  │  │     Outer Container (Privileged)           │    │ │  │
│  │  │  │     ghcr.io/coder/envbox:latest            │    │ │  │
│  │  │  │                                            │    │ │  │
│  │  │  │  ┌──────────────────────────────────────┐ │    │ │  │
│  │  │  │  │  Inner Container (Non-privileged)    │ │    │ │  │
│  │  │  │  │  codercom/enterprise-base:ubuntu     │ │    │ │  │
│  │  │  │  │                                      │ │    │ │  │
│  │  │  │  │  ┌────────────────────────────────┐ │ │    │ │  │
│  │  │  │  │  │     Coder Agent               │ │ │    │ │  │
│  │  │  │  │  │  - Claude Code (MCP)          │ │ │    │ │  │
│  │  │  │  │  │  - code-server (VS Code)      │ │ │    │ │  │
│  │  │  │  │  │  - Workspace Tools            │ │ │    │ │  │
│  │  │  │  │  └────────────────────────────────┘ │ │    │ │  │
│  │  │  │  │                                      │ │    │ │  │
│  │  │  │  │  ┌────────────────────────────────┐ │ │    │ │  │
│  │  │  │  │  │     Docker Daemon              │ │ │    │ │  │
│  │  │  │  │  │  - Build images                │ │ │    │ │  │
│  │  │  │  │  │  - Run containers              │ │ │    │ │  │
│  │  │  │  │  │  - Docker Compose              │ │ │    │ │  │
│  │  │  │  │  └────────────────────────────────┘ │ │    │ │  │
│  │  │  │  │                                      │ │    │ │  │
│  │  │  │  │  ┌────────────────────────────────┐ │ │    │ │  │
│  │  │  │  │  │     User Files & Projects      │ │ │    │ │  │
│  │  │  │  │  │  /home/coder/projects         │ │ │    │ │  │
│  │  │  │  │  └────────────────────────────────┘ │ │    │ │  │
│  │  │  │  └──────────────────────────────────────┘ │    │ │  │
│  │  │  └────────────────────────────────────────────┘    │ │  │
│  │  └─────────────────────────────────────────────────────┘ │  │
│  │                        ▲                                  │  │
│  │                        │                                  │  │
│  │                        │ Mounts                           │  │
│  │                        │                                  │  │
│  │  ┌─────────────────────┴─────────────────────────────┐   │  │
│  │  │     PersistentVolumeClaim (local-path)            │   │  │
│  │  │     - /home/coder                                 │   │  │
│  │  │     - Docker cache (/var/lib/docker)              │   │  │
│  │  │     - Docker images                               │   │  │
│  │  │     - Container storage                           │   │  │
│  │  └───────────────────────────────────────────────────┘   │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow

### 1. User Creates Workspace
```
User → Coder Dashboard → Coder Control Plane
                            ↓
                      Creates Pod in coder-workspaces
                            ↓
                      Binds PVC (local-path storage)
                            ↓
                      Starts Envbox container
                            ↓
                      Envbox starts inner container
                            ↓
                      Coder Agent connects back to Control Plane
                            ↓
                      Workspace ready! ✅
```

### 2. User Accesses Workspace
```
User → Coder Dashboard → WebSocket → Coder Agent (in workspace)
                                          ↓
                                    code-server (VS Code)
                                    windsurf, cursor, etc.
```

### 3. User Runs Docker Commands
```
User → Terminal in workspace → Docker CLI
                                    ↓
                              Docker Daemon (in inner container)
                                    ↓
                              Via Sysbox runtime (managed by envbox)
                                    ↓
                              Containers run (isolated by Sysbox)
```

## 📦 Storage Architecture

```
┌─────────────────────────────────────────────────────────────┐
│  K3s Node Filesystem                                        │
│  /var/lib/rancher/k3s/storage/                              │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  PVC: coder-<workspace-id>-home (30GB default)        │ │
│  │                                                       │ │
│  │  ├─ home/                                            │ │
│  │  │   └─ /home/coder (user files, git repos)         │ │
│  │  │                                                   │ │
│  │  ├─ cache/                                           │ │
│  │  │   ├─ docker/                                      │ │
│  │  │   │   └─ /var/lib/coder/docker                   │ │
│  │  │   │      (Docker build cache, volumes)           │ │
│  │  │   │                                               │ │
│  │  │   └─ containers/                                  │ │
│  │  │       └─ /var/lib/coder/containers               │ │
│  │  │          (Container storage)                      │ │
│  │  │                                                   │ │
│  │  └─ envbox/                                          │ │
│  │      ├─ docker/                                      │ │
│  │      │   └─ /var/lib/docker                         │ │
│  │      │      (Docker daemon storage, images)         │ │
│  │      │                                               │ │
│  │      └─ containers/                                  │ │
│  │          └─ /var/lib/containers                     │ │
│  │             (Envbox container storage)               │ │
│  └───────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  Pod Ephemeral Storage (EmptyDir - recreated on restart)   │
│                                                             │
│  /var/lib/sysbox                                            │
│  (Sysbox runtime state)                                     │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  Host Paths (Read-Only)                                     │
│                                                             │
│  /usr/src       → Kernel source                             │
│  /lib/modules   → Kernel modules                            │
└─────────────────────────────────────────────────────────────┘
```

## 🔒 Security Layers

```
┌─────────────────────────────────────────────────────────────┐
│  Layer 1: Kubernetes Namespace Isolation                    │
│  - RBAC controls who can create workspaces                  │
│  - Network policies (optional)                              │
│  - Resource quotas per namespace                            │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  Layer 2: Pod Security                                      │
│  - PodSecurityPolicies / PodSecurityAdmission               │
│  - Privileged containers controlled by admission            │
│  - Labels for tracking ownership                            │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  Layer 3: Envbox Container (Outer)                          │
│  - Runs privileged (required for Sysbox)                    │
│  - Isolated from other pods                                 │
│  - Limited to managing inner container                      │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  Layer 4: Sysbox Runtime                                    │
│  - Strong namespace isolation                               │
│  - Filesystem isolation                                     │
│  - Process isolation                                        │
│  - Network isolation                                        │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  Layer 5: Inner Container (Workspace)                       │
│  - Runs NON-PRIVILEGED                                      │
│  - User processes (UID 1000)                                │
│  - No direct access to host                                 │
│  - Docker access via Sysbox (not real privileged)           │
└─────────────────────────────────────────────────────────────┘
```

## 🔌 Network Flow

```
                    ┌──────────────────┐
                    │   User Browser   │
                    └────────┬─────────┘
                             │ HTTPS
                             ▼
                    ┌──────────────────┐
                    │  Ingress/LB      │
                    │  (Traefik/       │
                    │   klipper-lb)    │
                    └────────┬─────────┘
                             │
                             ▼
              ┌──────────────────────────────┐
              │    Coder Control Plane       │
              │    (Service: coder.coder)    │
              └────────┬──────────────┬──────┘
                       │              │
         Provisions    │              │ WebSocket/HTTP
         Workspaces    │              │
                       │              │
                       ▼              ▼
              ┌─────────────────────────────────┐
              │    Workspace Pods               │
              │    (coder-workspaces namespace) │
              │                                 │
              │  ┌───────────────────────────┐  │
              │  │   Pod: my-workspace       │  │
              │  │                           │  │
              │  │   ┌─────────────────────┐ │  │
              │  │   │  Coder Agent        │ │  │
              │  │   │  (connects back to  │ │  │
              │  │   │   Control Plane)    │ │  │
              │  │   └─────────────────────┘ │  │
              │  │                           │  │
              │  │   ┌─────────────────────┐ │  │
              │  │   │  code-server :8080  │ │  │
              │  │   └─────────────────────┘ │  │
              │  │                           │  │
              │  │   ┌─────────────────────┐ │  │
              │  │   │  Preview App :4200  │ │  │
              │  │   └─────────────────────┘ │  │
              │  └───────────────────────────┘  │
              └─────────────────────────────────┘
                             ▲
                             │
                      Exposed via
                      Coder subdomain
                      proxy (authenticated)
```

## 🔄 Lifecycle Management

```
┌──────────────────────────────────────────────────────────────┐
│                      Workspace Lifecycle                      │
└──────────────────────────────────────────────────────────────┘

1. CREATE
   ├─ User clicks "Create Workspace"
   ├─ Selects template: kubernetes-claude-dind
   ├─ Enters parameters (CPU, memory, disk, API key)
   └─ Coder provisions:
      ├─ PersistentVolumeClaim (local-path)
      ├─ Pod with Envbox
      └─ Waits for Agent connection
         └─ ✅ Workspace ready!

2. START (if stopped)
   ├─ User clicks "Start"
   ├─ Kubernetes recreates Pod
   ├─ Mounts existing PVC (data persists!)
   ├─ Envbox starts inner container
   ├─ Docker images still cached
   └─ ✅ Back where you left off!

3. STOP
   ├─ User clicks "Stop"
   ├─ Coder gracefully shuts down Agent
   ├─ Pod terminates
   ├─ PVC remains (data persists!)
   └─ ✅ Resources freed, data safe

4. DELETE
   ├─ User clicks "Delete"
   ├─ Pod deleted
   ├─ PVC deleted (data lost!)
   └─ ⚠️  Permanent deletion!

5. UPDATE
   ├─ Template changes pushed
   ├─ User clicks "Update"
   ├─ Pod recreated with new config
   ├─ PVC preserved (data safe!)
   └─ ✅ Running new template version
```

## 📊 Resource Usage

```
┌─────────────────────────────────────────────────────────────┐
│                     Resource Breakdown                       │
└─────────────────────────────────────────────────────────────┘

Workspace Pod:
┌────────────────┬──────────┬─────────┬──────────────────────┐
│   Component    │ Request  │  Limit  │     Description      │
├────────────────┼──────────┼─────────┼──────────────────────┤
│ Envbox         │ 500m CPU │ 4 CPU   │ Outer container      │
│ (Outer)        │ 1GB RAM  │ 8GB RAM │ (default settings)   │
├────────────────┼──────────┼─────────┼──────────────────────┤
│ Inner          │ Inherits │ Pass-   │ Workspace container  │
│ Container      │ from Pod │ through │ (Docker, tools, IDE) │
├────────────────┼──────────┼─────────┼──────────────────────┤
│ Storage        │   N/A    │  30GB   │ PVC (configurable)   │
│ (PVC)          │          │         │                      │
└────────────────┴──────────┴─────────┴──────────────────────┘

Typical Usage:
- Idle workspace:        ~500MB RAM, 0.1 CPU
- Active development:    ~2-4GB RAM, 1-2 CPU
- Docker build:          ~4-8GB RAM, 2-4 CPU
- Multiple containers:   ~8-12GB RAM, 3-6 CPU

Storage Usage:
- Base system:           ~2GB
- Docker images:         ~3-10GB (varies)
- User files:            ~1-5GB (varies)
- Docker cache:          ~2-5GB (grows over time)
- Recommended minimum:   30GB (template default)
```

## 🔧 Component Versions

```
┌─────────────────────────────────────────────────────────────┐
│                    Technology Stack                          │
└─────────────────────────────────────────────────────────────┘

Infrastructure:
├─ Kubernetes:             1.19+ (K3s compatible)
├─ Storage:                local-path (K3s default)
├─ Container Runtime:      containerd
└─ Ingress:                Traefik (K3s default)

Coder:
├─ Control Plane:          v2.27.1+
├─ PostgreSQL:             15
└─ Coder CLI:              Latest

Template:
├─ Outer Container:        ghcr.io/coder/envbox:latest
├─ Inner Container:        codercom/enterprise-base:ubuntu
├─ Sysbox:                 (embedded in envbox)
└─ Docker:                 (installed in inner container)

AI & IDE:
├─ Claude Code:            v3.0.0
├─ code-server:            Latest
├─ Windsurf:               Latest
├─ Cursor:                 Latest
└─ JetBrains:              Gateway integration

MCP Servers:
└─ desktop-commander:      Latest
```

## 🎯 Comparison with Alternatives

```
┌─────────────────────────────────────────────────────────────┐
│       kubernetes-claude-dind vs Other Solutions              │
└─────────────────────────────────────────────────────────────┘

┌──────────────────┬─────────────┬──────────────┬─────────────┐
│    Solution      │  Setup      │   Security   │  Features   │
├──────────────────┼─────────────┼──────────────┼─────────────┤
│ kubernetes-      │ ✅ Template │ ✅ Sysbox    │ ✅ DinD     │
│ claude-dind      │    Only     │    Isolation │ ✅ AI       │
│ (This Template)  │             │              │ ✅ All IDEs │
├──────────────────┼─────────────┼──────────────┼─────────────┤
│ Sysbox Runtime   │ ⚠️  Node    │ ✅ Best      │ ✅ DinD     │
│ (Direct)         │    Setup    │    Security  │ ❌ No AI    │
├──────────────────┼─────────────┼──────────────┼─────────────┤
│ Rootless Podman  │ ✅ Template │ ✅ No        │ ⚠️  Podman  │
│                  │    Only     │    Privileged│    Only     │
├──────────────────┼─────────────┼──────────────┼─────────────┤
│ Privileged       │ ✅ Template │ ❌ Full      │ ✅ DinD     │
│ Sidecar          │    Only     │    Privileged│ ⚠️  Risky   │
├──────────────────┼─────────────┼──────────────┼─────────────┤
│ Standard         │ ✅ Template │ ✅ Secure    │ ❌ No DinD  │
│ Container        │    Only     │              │ ✅ AI       │
└──────────────────┴─────────────┴──────────────┴─────────────┘

Winner: kubernetes-claude-dind (Best balance of ease + features + security)
```

---

**Visual guide complete! See other docs for implementation details.**
